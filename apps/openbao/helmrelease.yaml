apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: openbao
  namespace: openbao
spec:
  interval: 10m

  chart:
    spec:
      chart: openbao
      version: "0.29.4"
      sourceRef:
        kind: HelmRepository
        name: openbao
      interval: 10m

  values:
    replicaCount: 1
